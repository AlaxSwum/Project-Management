# 🚀 COPY AND PASTE THESE COMMANDS INTO YOUR SERVER TERMINAL
# Run these commands one by one on srv875725.hstgr.cloud

# Navigate to project directory
cd /var/www/project_management

# Reset any conflicts and pull latest changes
git reset --hard HEAD
git clean -fd
git pull origin main

# Stop the service
systemctl stop nextjs-pm

# Clear cache and rebuild
cd frontend
rm -rf .next node_modules/.cache node_modules package-lock.json
npm cache clean --force
npm install
npm run build

# Set permissions and restart
cd ..
chown -R www-data:www-data /var/www/project_management
chmod -R 755 /var/www/project_management
systemctl start nextjs-pm

# Check if service is running
systemctl status nextjs-pm

echo "✅ Deployment completed! Check https://srv875725.hstgr.cloud"
